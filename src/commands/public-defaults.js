/** Open to all server members (not admin-only). Channel still needs "Use Application Commands". */
export const publicSlashCommandDefaults = {
  default_member_permissions: null,
  dm_permission: false,
};
